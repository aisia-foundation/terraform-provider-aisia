---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "aisia_provider_key Resource - aisia"
subcategory: ""
description: |-
  Clé API d'un provider LLM spécifique à une organisation (isolation multi-tenant KEY-2). Validée par AISIA via un appel réel au provider avant stockage.
---

# aisia_provider_key (Resource)

Clé API d'un provider LLM **spécifique à une organisation** (isolation multi-tenant KEY-2). Validée par AISIA via un appel réel au provider avant stockage.

## Example Usage

```terraform
# Clé OpenAI propre à l'organisation ACME (isolation multi-tenant KEY-2).
resource "aisia_provider_key" "acme_openai" {
  org_id        = aisia_organization.acme.id
  provider_id   = "openai"
  key_value     = var.openai_key
  account_email = "billing@acme.example"
}

variable "openai_key" {
  type      = string
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `key_value` (String, Sensitive) Valeur de la clé API (sensible, write-only — non relue depuis l'API qui la masque).
- `org_id` (String) ID de l'organisation propriétaire de la clé.
- `provider_id` (String) ID du provider (ex. `openai`, `cohere`, `groq`).

### Optional

- `account_email` (String) Email du compte SaaS propriétaire de la clé (traçabilité).

### Read-Only

- `id` (String) Identifiant composite `org_id/provider_id`.

<!-- TF-DOCS-ENRICH:09_publications -->
## Documentation AISIA

- **Documentation produit** : [aisia.fr/docs](https://aisia.fr/docs)
- **Référence API OpenAPI** : [api.aisia.fr/docs](https://api.aisia.fr/docs)
- **Guide d'implémentation Terraform** : [guides/getting-started](guides/getting-started.md)
- **Provider registry** : [aisia-foundation/aisia](https://registry.terraform.io/providers/aisia-foundation/aisia/latest/docs)

> Ressource `resource` : `aisia_provider_key` — synchronisée avec l'OpenAPI AISIA.
